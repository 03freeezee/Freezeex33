function abouttarget() {
    const targetElement = document.getElementById('about');
    targetElement.scrollIntoView({ behavior: 'smooth' });
}

function introductiontarget() {
    const targetElement = document.getElementById('introduction');
    targetElement.scrollIntoView({ behavior: 'smooth' });
}


function skilltarget() {
    const targetElement = document.getElementById('skills');
    targetElement.scrollIntoView({ behavior: 'smooth' });
}


function abouttarget() {
    const targetElement = document.getElementById('about');
    targetElement.scrollIntoView({ behavior: 'smooth' });
}

function servicetarget() {
    const targetElement = document.getElementById('service-locator');
    targetElement.scrollIntoView({ behavior: 'smooth' });
}

